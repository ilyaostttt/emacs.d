#!/bin/bash

find . -type f \
     ! -name cscope.out \
     ! -name System.map \
     ! -name '*o.cmd'	\
     ! -name '.tmp_System.map'	\
     ! -name Module.symvers \
     -exec grep -nHI -e $@ {} +